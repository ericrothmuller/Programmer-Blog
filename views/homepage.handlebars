<main>
  <h2>Welcome to The Programmer Blog!</h2>
  {{#if loggedIn}}
  <div class="new-post">
    <form class="new-post">
      <h2>New Post</h2>
      <br />
      <label for="post-title">Post Title</label>
      <br />
      <input type="text" id="post-title" placeholder="Post Title">
      <br />
      <br />
      <label for="post-body">Post Body</label></p>
      <textarea id="post-body" name="post-body" rows="4" cols="50"></textarea>
      <input type="submit" value="Submit">
    </form>
  </div>
  <br />
  <hr />
  <br />
  {{#each posts as |post|}}
  {{display-post}}
  {{/each}}
  {{else}}
  <h2>Please Login</h2>
  {{/if}}
</main>